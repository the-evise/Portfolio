import FullScreenScroller, { FullScreenScrollSection } from "@/components/FullScreenScroller";
import Navigation from "@/components/Navigation";
import NestedScrollSection from "@/components/NestedScrollSection";
import AboutMeCard from "@/components/AboutMeCard";
import {NestedScrollArea} from "@/components/NestedScrollArea";

const nestedSteps = [
  { title: "Sketch Concepts", description: "Capture loose explorations to understand the interaction surface." },
  { title: "Prototype Motion", description: "Create quick motion studies that validate the feel before coding." },
  { title: "Refine Systems", description: "Translate the interaction into tokens, springs, and reusable components." },
  { title: "Polish Delivery", description: "Ship the experience with documentation, demos, and QA coverage." },
];

const sections: FullScreenScrollSection[] = [
  {
    id: "about",
    label: "About Me",
    className: "bg-gradient-to-br from-indigo-600 via-purple-600 to-blue-600 text-mint-cream",
    content: (
      <div className="text-center space-y-4 px-4">
        <p className="text-sm uppercase tracking-[0.3em] text-white/70">Designer · Engineer</p>
        <h1 className="text-5xl font-semibold">Motion-first storytelling</h1>
          <AboutMeCard/>
      </div>
    ),
  },
  {
    id: "components",
    label: "Components",
    className: "bg-night text-mint-cream",
    locksParent: true,
    content: (
      <div className="flex flex-col gap-6 px-6 md:px-12 w-full max-w-4xl">
        <h2 className="text-4xl font-medium">Component Library</h2>
          <NestedScrollArea>
              <div className="space-y-8 py-8">
                  {Array.from({ length: 40 }).map((_, i) => (
                      <p key={i} className="text-mint-cream">
                          Line {i + 1} — scroll me
                      </p>
                  ))}
              </div>
          </NestedScrollArea>
      </div>
    ),
  },
  {
    id: "work",
    label: "Work Process",
    className: "bg-mint-cream text-night",
    content: (
      <div className="flex flex-col gap-4 text-center px-4">
        <h2 className="text-4xl font-semibold">Work Process</h2>
        <p className="text-lg text-night/70">
          Nested scroll sections gate the snap scroll. You must reach the end of the inner content before advancing to
          the next full-screen card, mirroring the behavior you described.
        </p>
      </div>
    ),
  },
];

export default function TempPage() {
  return (
    <FullScreenScroller sections={sections}>
      <Navigation />
    </FullScreenScroller>
  );
}
